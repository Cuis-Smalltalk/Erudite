<?xml  version="1.0" encoding="utf-8" ?>
<book id="PetitParser tutorial" lang="en" version="5.0" xmlns="http://docbook.org/ns/docbook">
<title>PetitParser tutorial</title>
<chapter>
<title>Introduction</title>
<para>
<title>PetitParser: Building modular parsers</title></para>
<para></para>
<para>with the participation of:</para>
<para>
<emphasis>Jan Kurs</emphasis> 
<emphasis>(kurs@iam.unibe.ch)</emphasis></para>
<para></para>
<para>
<emphasis>Guillaume Larcheveque</emphasis> 
<emphasis>(guillaume.larcheveque@gmail.com)</emphasis></para>
<para></para>
<para>
<emphasis>Lukas Renggli</emphasis> 
<emphasis>(renggli@gmail.com)</emphasis></para>
<para></para>
<para>Building parsers to analyze and transform data is a common task in software development. In this chapter we present a powerful parser framework called PetitParser. PetitParser combines many ideas from various parsing technologies to model grammars and parsers as objects that can be reconfigured dynamically. PetitParser was written by Lukas Renggli as part of his work on the Helvetia system 1 but it can be used as a standalone library.</para>
<para></para></chapter>
<chapter>
<title>Writing parsers with PetitParser</title>
<para>
<title>Writing parsers with PetitParser</title></para>
<para></para>
<para>PetitParser is a parsing framework different from many other popular parser generators. PetitParser makes it easy to define parsers with Smalltalk code and to dynamically reuse, compose, transform and extend grammars. We can reflect on the resulting grammars and modify them on-the-fly. As such PetitParser fits better the dynamic nature of Smalltalk.Furthermore, PetitParser is not based on tables such as SmaCC and ANTLR. Instead it uses a combination of four alternative parser methodologies: scannerless parsers, parser combinators, parsing expression grammars and packrat parsers. As such PetitParser is more powerful in what it can parse. </para>
<para>Let's have a quick look at these four parser methodologies:</para>
<para></para>
<para>
<emphasis>Scannerless Parsers</emphasis> combine what is usually done by two independent tools (scanner and parser) into one. This makes writing a grammar much simpler and avoids common problems when grammars are composed.</para>
<para>
<emphasis>Parser Combinators</emphasis> are building blocks for parsers modeled as a graph of composable objects; they are modular and maintainable, and can be changed, recomposed, transformed and reflected upon.</para>
<para>
<emphasis>Parsing Expression Grammars</emphasis> (PEGs) provide the notion of ordered choices. Unlike parser combinators, the ordered choice of PEGs always follows the first matching alternative and ignores other alternatives. Valid input always results in exactly one parse-tree, the result of a parse is never ambiguous.</para>
<para>
<emphasis>Packrat Parsers</emphasis> give linear parse-time guarantees and avoid common problems with left-recursion in PEGs.</para>
<section>
<title>Writing a simple grammar</title>
<para>
<title>Writing a simple grammar</title></para>
<para></para>
<para>Writing grammars with PetitParser is as simple as writing Smalltalk code. For example, to define a grammar that parses identifiers starting with a letter followed by zero or more letters or digits is defined and used as follows:</para>
<para></para>
<para>
<programlisting>identifier := #letter asParser , #word asParser star.&#10;identifier parse: 'a987jlkj'&#10;</programlisting></para></section>
<section>
<title>Parsing some input</title>
<para>
<title>Parsing some input</title></para>
<para></para>
<para>To actually parse a string (or stream) we use the method 
<ulink url="PPParser&gt;&gt;parse: ">PPParser&gt;&gt;parse: </ulink> as follows:</para>
<para></para>
<para>
<programlisting>identifier parse: 'yeah'</programlisting></para>
<para></para>
<para>
<programlisting>identifier parse: 'f123'</programlisting></para>
<para></para>
<para>While it seems odd to get these nested arrays with characters as a return value, this is the default decomposition of the input into a parse tree. We'll see in a while how that can be customized. If we try to parse something invalid we get an instance of 
<ulink url="PPFailure">PPFailure</ulink> as an answer:</para>
<para></para>
<para>
<programlisting>identifier parse: '123'</programlisting></para>
<para></para>
<para>This parsing results in a failure because the first character ( 1 ) is not a letter. Instances of PPFailure are the only objects in the system that answer with true when you send the message 
<ulink url="isPetitFailure">isPetitFailure</ulink>. Alternatively you can also use 
<ulink url="PPParser&gt;&gt;parse:onError: ">PPParser&gt;&gt;parse:onError: </ulink> to throw an exception in case of an error:</para>
<para></para>
<para>
<programlisting>identifier&#10;   parse: '123'&#10;   onError: [ :msg :pos | self error: msg ] </programlisting></para>
<para></para>
<para>If you are only interested if a given string (or stream) matches or not you can use the following constructs:</para>
<para></para>
<para>
<programlisting>identifier matches: 'foo'</programlisting></para>
<para></para>
<para>
<programlisting>identifier matches: '123'</programlisting></para>
<para></para>
<para>
<programlisting>identifier matches: 'foo()'</programlisting></para>
<para></para>
<para>The last result can be surprising: indeed, a parenthesis is neither a digit nor a letter as was specified by the 
<programlisting>#word asParser</programlisting> expression. In fact, the identifier parser matches 
<programlisting>'foo'</programlisting> and this is enough for the 
<ulink url="PPParser&gt;&gt;matches: ">PPParser&gt;&gt;matches: </ulink> call to return true . The result would be similar with the use of parse: which would return 
<programlisting>#($f #($o $o))</programlisting> .If you want to be sure that the complete input is matched, use the message 
<ulink url="PPParser&gt;&gt;end ">PPParser&gt;&gt;end </ulink> as follows:</para>
<para></para>
<para>
<programlisting>identifier end matches:'foo'</programlisting></para>
<para></para>
<para>The 
<ulink url="PPParser&gt;&gt;end ">PPParser&gt;&gt;end </ulink> message creates a new parser that matches the end ofinput. To be able to compose parsers easily, it is important that parsers do not match the end of input by default. Because of this, you might be interested to find all the places that a parser can match using the message 
<ulink url="PPParser&gt;&gt;matchesSkipIn: ">PPParser&gt;&gt;matchesSkipIn: </ulink> and 
<ulink url="PPParser&gt;&gt;matchesIn: ">PPParser&gt;&gt;matchesIn: </ulink>.</para>
<para></para>
<para>
<programlisting>identifier matchesSkipIn: 'foo 123 bar12'</programlisting></para>
<para></para>
<para>
<programlisting>identifier matchesIn: 'foo 123 bar12'</programlisting></para>
<para></para>
<para>The 
<ulink url="PPParser&gt;&gt;matchesSkipIn: ">PPParser&gt;&gt;matchesSkipIn: </ulink> method returns a collection of arrays containing what has been matched. This function avoids parsing the same character twice. The method 
<ulink url="PPParser»matchesIn: ">PPParser»matchesIn: </ulink> does a similar job but returns a collection with all possible sub-parsed elements: e.g., evaluating 
<programlisting>identifier matchesIn: 'foo 123 bar12'</programlisting> returns a collection of 6 elements.</para>
<para>Similarly, to find all the matching ranges (index of first character andindex of last character) in the given input one can use either 
<ulink url="PPParser&gt;&gt;matchingSkipRangesIn: ">PPParser&gt;&gt;matchingSkipRangesIn: </ulink> or 
<ulink url="PPParser&gt;&gt;matchingRangesIn: ">PPParser&gt;&gt;matchingRangesIn: </ulink> as shown by the script below:</para>
<para></para>
<para>
<programlisting>identifier matchingSkipRangesIn: 'foo 123 bar12'</programlisting></para>
<para></para>
<para>
<programlisting>identifier matchingRangesIn: 'foo 123 bar12'</programlisting></para>
<para></para>
<para></para></section>
<section>
<title>Different kinds of parsers</title>
<para>
<title>Different kinds of parsers</title></para>
<para></para>
<para>PetitParser provide a large set of ready-made parser that you can compose to consume and transform arbitrarily complex languages. The terminal parsers are the most simple ones. We've already seen a few of those, some more are defined in the protocol Table 1.1.</para>
<para>The class side of 
<ulink url="PPPredicateObjectParser">PPPredicateObjectParser</ulink> provides a lot of other factory methods that can be used to build more complex terminal parsers. To use them, send the message 
<ulink url="PPParser&gt;&gt;asParser ">PPParser&gt;&gt;asParser </ulink> to a symbol containing the name of the factory method (such as 
<programlisting>#punctuation asParser</programlisting> ).</para>
<para>The next set of parsers are used to combine other parsers together and isdefined in the protocol:</para>
<para></para>
<para>
<emphasis>Terminal Parsers</emphasis></para>
<para></para>
<para>
<programlisting>$a asParser</programlisting>. Parses the character $a.</para>
<para>
<programlisting>'abc' asParser</programlisting>. Parses the string 'abc'.</para>
<para>
<programlisting>#any asParser</programlisting>. Parses any character.</para>
<para>
<programlisting>#digit asParser</programlisting>. Parses one digit (0..9).</para>
<para>
<programlisting>#letter asParser</programlisting>. Parses one letter (a..z and A..Z).</para>
<para>
<programlisting>#word asParser</programlisting>. Parses a digit or letter.</para>
<para>
<programlisting>#blank asParser</programlisting>. Parses a space or a tabulation.</para>
<para>
<programlisting>#newline asParser</programlisting>. Parses the carriage return or line feed characters.</para>
<para>
<programlisting>#space asParser</programlisting>. Parses any white space character including new line.</para>
<para>
<programlisting>#tab asParser</programlisting>. Parses a tab character.</para>
<para>
<programlisting>#lowercase asParser</programlisting>. Parses a lowercase character.</para>
<para>
<programlisting>#uppercase asParser</programlisting>. Parses an uppercase character.</para>
<para>
<programlisting>nil asParser</programlisting>. Parses nothing.</para>
<para></para>
<para>
<emphasis>Parser Combinators</emphasis></para>
<para></para>
<para>
<emphasis>p1 , p2</emphasis>. Parses p1 followed by p2 (sequence).</para>
<para>
<emphasis>p1 / p2</emphasis>. Parses p1, if that doesn't work parses p2.</para>
<para>
<emphasis>p star</emphasis>. Parses zero or more p.</para>
<para>
<emphasis>p plus</emphasis>. Parses one or more p.</para>
<para>
<emphasis>p optional</emphasis>. Parses p if possible.</para>
<para>
<emphasis>p and</emphasis>. Parses p but does not consume its input.</para>
<para>
<emphasis>p negate</emphasis>. Parses p and succeeds when p fails.</para>
<para>
<emphasis>p not</emphasis>. Parses p and succeeds when p fails, but does not consume its input.</para>
<para>
<emphasis>p end</emphasis>. Parses p and succeeds only at the end of the input.</para>
<para>
<emphasis>p times: n</emphasis>. Parses p exactly n times.</para>
<para>
<emphasis>p min: n max: m</emphasis>. Parses p at least n times up to m times</para>
<para>
<emphasis>p starLazy: q</emphasis>. Like  star  but  stop  consumming  when  q  suc-ceeds</para>
<para>As a simple example of parser combination, the following definition of the</para>
<para>
<emphasis>identifier2 parser</emphasis> is equivalent to our previous definition of 
<emphasis>identifier</emphasis>:</para>
<para></para>
<para>
<programlisting>identifier2 := #letter asParser , (#letter asParser / #digit asParser) star</programlisting></para>
<para></para>
<para></para>
<para>
<title>Parser action</title></para>
<para></para>
<para>To define an action or transformation on a parser we can use one of the mes-sages 
<ulink url="PPParser&gt;&gt;==&gt; ">PPParser&gt;&gt;==&gt; </ulink>, 
<ulink url="PPParser&gt;&gt;flatten ">PPParser&gt;&gt;flatten </ulink>, 
<ulink url="PPParser&gt;&gt;token ">PPParser&gt;&gt;token </ulink> and 
<ulink url="PPParser&gt;&gt;trim ">PPParser&gt;&gt;trim </ulink> defined in the protocol:</para>
<para></para>
<para>
<emphasis>Action parsers</emphasis></para>
<para></para>
<para>
<emphasis>p flatten</emphasis>. Creates a string from the result of p.</para>
<para>
<emphasis>p token</emphasis>. Similar to flatten but returns a 
<ulink url="PPToken">PPToken</ulink> with details.</para>
<para>
<emphasis>p trim</emphasis>. Trims white spaces before and after p.</para>
<para>
<emphasis>p trim: trimParser</emphasis>. Trims whatever trimParser can parse (e.g., comments).</para>
<para>
<emphasis>p ==&gt; aBlock</emphasis>. Performs the transformation given in aBlock.</para>
<para>To return a string of the parsed identifier instead of getting an array of matched elements, configure the parser by sending it the message 
<ulink url="PPParser&gt;&gt;flatten ">PPParser&gt;&gt;flatten </ulink>.</para>
<para></para>
<para>
<programlisting>|identifier|&#10;identifier := (#letter asParser , (#letter asParser / #digit asParser) star).&#10;identifier parse: ' ajka0 '</programlisting></para>
<para></para>
<para></para>
<para>
<programlisting>|identifier|&#10;identifier := (#letter asParser , (#letter asParser / #digit asParser) star).&#10;identifier parse: 'ajka0'</programlisting></para>
<para></para>
<para>Sending the message trim is equivalent to calling 
<ulink url="PPParser&gt;&gt;trim: ">PPParser&gt;&gt;trim: </ulink> with 
<programlisting>#space asParser</programlisting> as a parameter. That means 
<emphasis>trim:</emphasis> can be useful to ignore other data from the input, source code comments for example:</para>
<para></para>
<para>
<programlisting>| identifier comment ignorable line |&#10;identifier := (#letter asParser , #word asParser star) flatten.&#10;comment := '//' asParser, #newline asParser negate star.&#10;ignorable := comment / #space asParser.&#10;line := identifier trim: ignorable.&#10;line parse: '// This is a comment&#10;oneIdentifier // another comment'</programlisting></para>
<para></para>
<para>The message PPParser»==&gt; lets you specify a block to be executed when the parser matches an input. The next section presents several examples. Here is a simple way to get a number from its string representation.</para>
<para></para>
<para>
<programlisting>number := #digit asParser plus flatten</programlisting></para>
<para></para>
<para>
<programlisting>number parse: '123'</programlisting></para>
<para></para>
<para>The table 1.3 shows the basic elements to build parsers. There are a few more well documented and tested factory methods in the operators protocols of PPParser . If you want to know more about these factory methods, browse these protocols. An interesting one is separatedBy: which answers a new parser that parses the input one or more times, with separations specified by another parser.</para>
<para></para></section>
<section>
<title>Writing a more complicated grammar</title>
<para>
<title>Writing a more complicated grammar</title></para>
<para></para>
<para>We now write a more complicated grammar for evaluating simple arithmetic expressions. With the grammar for a number (actually an integer) defined above, the next step is to define the productions for addition and multiplication in order of precedence. Note that we instantiate the productions as PPDelegateParser upfront, because they recursively refer to each other. The method #setParser: then resolves this recursion. The following script defines three parsers for the addition, multiplication and parenthesis (see Figure 1.4 for the related syntax diagram):</para>
<para></para>
<para>
<programlisting>&#10;number := #digit asParser plus flatten ==&gt; [:node | node asNumber].&#10;term := PPDelegateParser new.&#10;prod := PPDelegateParser new.&#10;prim := PPDelegateParser new.&#10;term setParser: (prod , $+ asParser trim , term ==&gt; [ :nodes | nodes first  + nodes last  ])&#10;/ prod.&#10;prod setParser: (prim , $* asParser trim , prod ==&gt; [ :nodes | nodes first  * nodes last  ])&#10;/ prim.&#10;prim setParser: ($( asParser trim , term , $) asParser trim ==&gt; [ :nodes | nodes second ])&#10;/ number.</programlisting></para>
<para></para>
<para>The term parser is defined as being either (1) a prod followed by '+', followed by another term or (2) a prod. In case (1), an action block asks the parser to compute the arithmetic addition of the value of the first node (a prod) and the last node (a term). The prod parser is similar to the term parser. The prim parser is interesting in that it accepts left and right parenthesis before and after a term and has an action block that simply ignores them.</para>
<para>To understand the precedence of productions, see Figure 1.5. The root of the tree in this figure ( term ), is the production that is tried first. A term is either a + or a prod . The term production comes first because + as the lowest priority in mathematics.</para>
<para>To make sure that our parser consumes all input we wrap it with the end parser into the start production:</para>
<para></para>
<para>
<programlisting>start := term end</programlisting></para>
<para></para>
<para>That's it, we can now test our parser:</para>
<para></para>
<para>
<programlisting>start parse: '1 + 2 * 3'</programlisting></para>
<para></para>
<para>
<programlisting>start parse: '(1 + 2) * 3'</programlisting></para></section></chapter>
<chapter>
<title>Composite grammars with PetitParser</title>
<para>
<title>Composite grammars with PetitParser</title></para>
<para></para>
<para>In the previous section we saw the basic principles of PetitParser and gave some introductory examples. In this section we are going to present a way to define more complicated grammars. We continue where we left off with the arithmetic expression rammar.Writing parsers as a script as we did previously can be  cumbersome, especially when grammar productions are mutually recursive and refer to each other in complicated ways. Furthermore a grammar specified in a single script makes it unnecessary hard to reuse specific parts of that grammar.Luckily there is 
<ulink url="PPCompositeParser">PPCompositeParser</ulink> to the rescue.</para>
<section>
<title>Defining the grammar</title>
<para>
<title>Defining the grammar</title></para>
<para></para>
<para>As an example let's create a composite parser using the same expression grammar we built in the last section but this time we define it inside a class subclass of 
<ulink url="PPCompositeParser ">PPCompositeParser </ulink>.</para>
<para></para>
<para>
<ulink url="ExpressionGrammar">ExpressionGrammar</ulink></para>
<para></para>
<para>Again we start with the grammar for an integer number. Define themethod number as follows:</para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;number ">ExpressionGrammar&gt;&gt;number </ulink></para>
<para></para>
<para>Every production in ExpressionGrammar is specified as a method that returns its parser. Similarly, we define the productions term , prod , mul , and prim . Productions refer to each other by reading the respective instance variable of the same name and PetitParser takes care of initializing these instance variables for you automatically. We let Smalltalk automatically add the necessary instance variables as we refer to them for the first time. We obtain the following class definition:</para>
<para></para>
<para>
<ulink url="ExpressionGrammar">ExpressionGrammar</ulink></para>
<para></para>
<para>Define more expression grammar parsers, this time with no associated action:</para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;term ">ExpressionGrammar&gt;&gt;term </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;add ">ExpressionGrammar&gt;&gt;add </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;prod ">ExpressionGrammar&gt;&gt;prod </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;mul ">ExpressionGrammar&gt;&gt;mul </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;prim ">ExpressionGrammar&gt;&gt;prim </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;parens ">ExpressionGrammar&gt;&gt;parens </ulink></para>
<para></para>
<para>Contrary to our previous implementation we do not define the production actions yet (what we previously did by using PPParser»==&gt; ); and we factor out the parts for addition ( add ), multiplication ( mul ), and parenthesis ( parens ) into separate productions. This will give us better reusability later on. For example, a subclass may override such methods to produce slightly different production output. Usually, production methods are categorized in a protocol named grammar (which can be refined into more specific protocol names when necessary such as grammar - literals ).</para>
<para>Last but not least we define the starting point of the expression grammar. This is done by overriding 
<ulink url="PPCompositeParser&gt;&gt;start ">PPCompositeParser&gt;&gt;start </ulink> in the ExpressionGrammar class:</para>
<para></para>
<para>
<ulink url="ExpressionGrammar&gt;&gt;start ">ExpressionGrammar&gt;&gt;start </ulink></para>
<para></para>
<para>Instantiating the ExpressionGrammar gives us an expression parser that returns a default abstract-syntax tree:</para>
<para></para>
<para>
<programlisting>parser := ExpressionGrammar new.</programlisting></para>
<para></para>
<para>
<programlisting>parser parse: '1 + 2 * 3'</programlisting></para>
<para></para>
<para>
<programlisting>parser parse: '(1 + 2) * 3'</programlisting></para></section>
<section>
<title>Writing dependent grammars</title>
<para>
<title>Writing dependent grammars</title></para>
<para></para>
<para>You can easily reuse parsers defined by other grammars. For example, imag-ine you want to create a new grammar that reuses the definition of number inthe ExpressionGrammar we have just defined. For this, you have to declare adependency to ExpressionGrammar:</para>
<para></para>
<para>
<programlisting>PPCompositeParser subclass: #MyNewGrammar&#10;instanceVariableNames: 'number'&#10;classVariableNames: ''&#10;poolDictionaries: ''&#10;category: 'PetitTutorial'&#10;&#10;MyNewGrammar class&gt;&gt;dependencies&#10;&quot;Answer a collection of PPCompositeParser classes that this parser directly&#10;dependends on.&quot;&#10;^ {ExpressionGrammar}&#10;&#10;MyNewGrammar&gt;&gt;number&#10;&quot;Answer the same parser as ExpressionGrammar&gt;&gt;number.&quot;&#10;^ (self dependencyAt: ExpressionGrammar) number</programlisting></para></section>
<section>
<title>Defining an evaluator</title>
<para>
<title>Defining an evaluator</title></para>
<para></para>
<para>Now that we have defined a grammar we can reuse this definition to imple-ment an evaluator. To do this we create a subclass of 
<ulink url="ExpressionGrammar">ExpressionGrammar</ulink> called</para>
<para>
<ulink url="ExpressionEvaluator">ExpressionEvaluator</ulink>:</para>
<para></para>
<para>
<ulink url="ExpressionEvaluator">ExpressionEvaluator</ulink></para>
<para></para>
<para>We then redefine the implementation of add , mul and parens with our eval-uation semantics. This is accomplished by calling the super implementationand adapting the returned parser as shown in the following methods:</para>
<para></para>
<para>
<ulink url="ExpressionEvaluator&gt;&gt;add ">ExpressionEvaluator&gt;&gt;add </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionEvaluator&gt;&gt;mul ">ExpressionEvaluator&gt;&gt;mul </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionEvaluator&gt;&gt;parens ">ExpressionEvaluator&gt;&gt;parens </ulink></para>
<para></para>
<para>The evaluator is now ready to be tested:</para>
<para></para>
<para>
<programlisting>parser := ExpressionEvaluator new.</programlisting></para>
<para></para>
<para>
<programlisting>parser parse: '1 + 2 * 3'.</programlisting></para>
<para></para>
<para>
<programlisting>parser parse: '(1 + 2) * 3'.</programlisting></para></section>
<section>
<title>Defining a Pretty-Printer</title>
<para>
<title>Defining a Pretty-Printer</title></para>
<para></para>
<para>We can reuse the grammar for example to define a simple pretty printer. Thisis as easy as subclassing ExpressionGrammar again!</para>
<para></para>
<para>
<ulink url="ExpressionPrinter">ExpressionPrinter</ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionPrinter&gt;&gt;add ">ExpressionPrinter&gt;&gt;add </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionPrinter&gt;&gt;mul ">ExpressionPrinter&gt;&gt;mul </ulink></para>
<para></para>
<para></para>
<para>
<ulink url="ExpressionPrinter&gt;&gt;parens ">ExpressionPrinter&gt;&gt;parens </ulink></para>
<para></para>
<para>This pretty printer can be tried out as shown by the following expressions:</para>
<para></para>
<para>
<programlisting>parser := ExpressionPrinter new.</programlisting></para>
<para></para>
<para>
<programlisting>parser parse: '1+2 *3'.</programlisting></para>
<para></para>
<para>
<programlisting>parser parse: '(1+ 2   )* 3'.</programlisting></para></section>
<section>
<title>Easy expressions with PPExpressionParser</title>
<para>
<title>Easy expressions with PPExpressionParser</title></para>
<para></para>
<para>PetitParser proposes a powerful tool to create expressions; PPExpressionParseris a parser to conveniently define an expression grammar with prefix, postfix, and left- and right-associative infix operators. The operator-groups are defined in descending precedence.</para>
<para>The ExpressionGrammar we previously defined can be implemented infew lines:</para>
<para>
<programlisting>&#10;| expression parens number |&#10;expression := PPExpressionParser new.&#10;parens := $( asParser token trim , expression , $) asParser token trim&#10;==&gt; [ :nodes | nodes second ].&#10;number := #digit asParser plus flatten trim ==&gt; [ :str | str asNumber ].&#10;expression term: parens / number.&#10;expression&#10;&#9;group: [ :g |&#10;&#9;&#9;g left: $* asParser token trim do: [ :a :op :b | a * b ].&#10;&#9;&#9;g left: $/ asParser token trim do: [ :a :op :b | a / b ] ];&#10;&#9;group: [ :g |&#10;&#9;&#9;g left: $+ asParser token trim do: [ :a :op :b | a + b ].&#10;&#9;&#9;g left: $ - asParser token trim do: [ :a :op :b | a - b ] ]. &#10;</programlisting></para>
<para>Now our parser is also able to manage subtraction and division:</para>
<para>
<programlisting>expression parse: '1 - 2/3'.</programlisting></para>
<para></para>
<para>How do you decide when to create a subclass of PPCompositeParser orinstantiate PPExpressionParser ? On the one hand, you should instantiate aPPExpressionParser if you want to do a small parser for a small task. On theother hand, if you have a grammar that's composed of many parsers, youshould subclass PPCompositeParser .</para>
<para></para></section></chapter></book>